{
  "author": "Artem Tarasov",
  "description": "Technical Assignment for QA Engineers",
  "scripts": {
    "cypress:open": "cypress open",
    "chrome:report": "cypress run --spec 'cypress/integration/**' --headless --browser chrome",
    "firefox:report": "cypress run --spec 'cypress/integration/**' --headless --browser firefox",
    "edge:report": "cypress run --spec 'cypress/integration/**' --headless --browser edge"
  },
  "dependencies": {
    "cypress": "^9.2.0"
  },
  "devDependencies": {
    "mochawesome": "^7.0.1",
    "mochawesome-merge": "^4.2.1"
  }
}
